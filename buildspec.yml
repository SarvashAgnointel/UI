version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 18  # Adjust based on your Node.js version
    commands:
      - npm install
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - echo "Build completed"
artifacts:
  files:
    - '**/*'
